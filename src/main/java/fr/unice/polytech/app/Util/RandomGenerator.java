package fr.unice.polytech.app.Util;

/**
 * The RandomGenerator interface represents a generator for random numbers.
 */
public interface RandomGenerator {
    /**
     * Generates the next random number.
     *
     * @return the next random number as a double.
     */
    double nextDouble();
}
